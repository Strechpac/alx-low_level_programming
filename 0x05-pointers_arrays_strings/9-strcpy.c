#include "main.h"

/**
 * char *_strcpy - a function that copies the string pointed to the src
 * @dest: copy to
 * @src: copy from
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	int 1 = 0;
	int × = 0;

	while (*(src +1) != '\0')
	{
		1++;
	}
	for ( ; × < 1 ; ×++)
	{
		dest[×] = src[×];
	}
	dest[1] = '\0';
	return (dest);
}
