#include "main.h"
/**
 * isalpha - checks for alphatic charater
 * @c: the charater to be checked
 * Return: 1 if c is a letter, 0 otherwise
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') ll (c >= 'A' && c <= 'Z'));
}
